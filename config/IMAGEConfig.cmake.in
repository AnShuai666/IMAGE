find_path(IMAGE_INCLUDE_DIRS NAMES IMAGEConfig.h PATHS "${PROJECT_SOURCE_DIR}/include")
find_library(IMAGE_LIBRARIES NAMES image PATHS "${PROJECT_SOURCE_DIR}/lib")

unset(IMAGE_INLCUDE_DIRS)
unset(IMAGE_LIBRARIES)
set(IMAGE_INCLUDE_DIRS "/home/as/IMAGE/include")
find_library(IMAGE_LIBRARIES image PATHS "/home/as/IMAGE/lib")

set(IMAGE_FOUND TRUE)

set(IMAGE_INCLUDE_DIR ${IMAGE_INCLUDE_DIRS})
set(IMAGE_LIBS ${IMAGE_LIBRARIES})
set(IMAGE_LIBRARY ${IMAGE_LIBRARIES})